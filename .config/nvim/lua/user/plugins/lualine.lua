require('lualine').setup({
    options = {
        theme = 'catppuccin', -- catppuccin, nightfly
    },
    -- sections = {
    --     lualine_c = {
    --         require('auto-session-library').current_session_name
    --     }
    -- }
})
